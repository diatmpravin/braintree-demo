<h1>Listing orders</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Price</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>    
  </tr>

<% @orders.each do |order| %>
  <tr>
    <td><%= order.name %></td>
    <td><%= order.price %></td>    
    <td><%= link_to 'Edit', edit_order_path(order) %></td>    
    <td></td>
    <td></td>
    <td><%= link_to 'Buy', payment_new_path(:amount => order.price) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Order', new_order_path %>
